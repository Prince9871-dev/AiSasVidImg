export interface Video {
    publicId: string;
    title: string;
    duration: number;
    size: number;
    originalSize: number;
    compressedSize: number;
    description: string;
    createdAt: string;
}