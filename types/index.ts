export interface Video {
    id: string;
    publicId: string;
    title: string;
    duration: number;
    size: number;
    originalSize: number;
    compressedSize: number;
    description: string;
    createdAt: string;
}